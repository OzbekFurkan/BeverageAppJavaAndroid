This is a beverage app store app. MySql and JDBC are used in this project. Database is exported with a few data row inside them (BeverageAppDB.sql). 
Inport the .sql file in your database and set your configurations such as post, ip etc.
Beverage datas need to be added in database side. Images should be cloud image link.
